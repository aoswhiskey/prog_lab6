package common.network.requests;

import common.utility.Commands;

public class MaxByMinimalPointRequest extends Request {
    public MaxByMinimalPointRequest() {
        super(Commands.MAX_BY_MINIMAL_POINT);
    }
}
